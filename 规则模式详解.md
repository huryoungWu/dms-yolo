# 疲劳驾驶检测系统 — 规则模式详解

## 一、整体流程

规则模式不使用任何深度学习模型，纯靠几何计算 + 阈值判断。流程如下：

```
摄像头每一帧 (BGR图像)
    │
    ▼
① FaceDetector.detect(frame) → 468个面部关键点
    │
    ├── ② EyeAnalyzer.analyze(left_eye, right_eye) → EyeResult
    ├── ③ MouthAnalyzer.analyze(mouth) → MouthResult
    └── ④ HeadPoseAnalyzer.estimate_pose(head_points, shape) → PoseResult
                                                      │
                                                      ▼
                                            ⑤ FatigueEvaluator.evaluate()
                                                      │
                                                      ▼
                                              ⑥ 渲染 & 预警
```

---

## 二、第①步：人脸关键点检测（FaceDetector）

**文件**：`detectors/face_detector.py`

**算法**：MediaPipe FaceMesh（Google 的预训练模型）

**不需要数据集** — MediaPipe 本身就是预训练好的，直接调用。

**做了什么**：

- 输入：一帧 BGR 图像（比如 640×480）
- 将 BGR 转为 RGB，送入 MediaPipe FaceMesh
- 输出：468 个人脸关键点，每个点是归一化坐标 (0\~1)
- 项目将其转为像素坐标 `(x * w, y * h)`

**关键点提取**（硬编码索引）：

```python
# 左眼 6 个点
LEFT_EYE_INDICES = [33, 160, 158, 133, 153, 144]
# 右眼 6 个点
RIGHT_EYE_INDICES = [362, 385, 387, 263, 373, 380]
# 嘴巴 6 个点
MOUTH_INDICES = {
    "upper": 13, "lower": 14, "left": 78,
    "right": 308, "upper_inner": 82, "lower_inner": 312
}
# 头部姿态 6 个点
HEAD_POSE_INDICES = {
    "nose_tip": 1, "chin": 152, "left_eye_corner": 33,
    "right_eye_corner": 263, "left_mouth": 61, "right_mouth": 291
}
```

**返回**：`FaceLandmarks` 数据类，包含左眼、右眼、嘴巴、头部姿态的关键点。

---

## 三、第②步：眼睛分析（EyeAnalyzer）

**文件**：`detectors/eye_analyzer.py`

**算法**：EAR（Eye Aspect Ratio，眼睛纵横比），来自论文 *"Real-Time Eye Blink Detection using Facial Landmarks"* (Soukupová & Čech, 2016)

**不需要数据集**，纯几何计算。

### EAR 计算公式

用 6 个眼部关键点，按顺序编号 p1\~p6（沿着眼睛轮廓）：

```
        p2    p3
        |      |
p1 ─────┼──────┼───── p4    ← 水平轴
        |      |
        p6    p5
```

```python
vertical_1 = distance(p2, p6)    # 垂直距离1
vertical_2 = distance(p3, p5)    # 垂直距离2
horizontal = distance(p1, p4)    # 水平距离

EAR = (vertical_1 + vertical_2) / (2.0 * horizontal)
```

**直觉**：

- 睁眼时：垂直距离大，EAR ≈ 0.25\~0.3
- 闭眼时：垂直距离趋近 0，EAR ≈ 0.05 以下
- 人种差异、摄像头角度不影响，因为是**比值**，具有尺度不变性

### 疲劳判断逻辑

```python
left_ear = calculate_ear(left_eye_points)
right_ear = calculate_ear(right_eye_points)
avg_ear = (left_ear + right_ear) / 2.0    # 双眼取平均

is_closed = avg_ear < 0.2                  # 低于阈值 → 闭眼

if is_closed:
    _frame_counter += 1                    # 连续闭眼帧数 +1
else:
    _frame_counter = 0                     # 一睁眼就重置

is_fatigued = _frame_counter >= 48         # 连续 48 帧（~1.6秒 @30fps）→ 疲劳
```

**关键设计**：不是一闭眼就报警，而是要求**连续**闭眼超过一定帧数。这避免了正常眨眼的误报。

**输入**：左右眼各 6 个像素坐标点
**输出**：`EyeResult(ear=0.15, is_closed=True, is_fatigued=True, frame_count=52)`

---

## 四、第③步：嘴巴分析（MouthAnalyzer）

**文件**：`detectors/mouth_analyzer.py`

**算法**：MAR（Mouth Aspect Ratio，嘴巴纵横比），类似 EAR 的思路。

### MAR 计算公式

用 6 个嘴部关键点：

```
    upper_inner
    ───────────
    upper (上唇)
    ═══════════  ← 张开
    lower (下唇)
    ───────────
    lower_inner

left ←────────→ right  (水平宽度)
```

```python
horizontal = distance(left, right)                  # 嘴巴宽度
vertical_inner = distance(upper_inner, lower_inner) # 内圈垂直距离
vertical_outer = distance(upper, lower)             # 外圈垂直距离

MAR = (vertical_inner + vertical_outer) / (2.0 * horizontal)
```

**直觉**：

- 正常闭嘴：MAR 较小（垂直距离小）
- 打哈欠：嘴巴张大，垂直距离剧增，MAR > 0.75

### 疲劳判断逻辑

```python
mar = calculate_mar(mouth_points)
is_yawning = mar > 0.75                    # 超过阈值 → 张嘴

if is_yawning:
    _frame_counter += 1
else:
    _frame_counter = 0

is_fatigued = _frame_counter >= 25         # 连续 25 帧（~0.83秒 @30fps）→ 哈欠疲劳
```

**输入**：嘴巴 6 个关键点坐标（字典形式）
**输出**：`MouthResult(mar=0.82, is_yawning=True, is_fatigued=True, frame_count=30)`

---

## 五、第④步：头部姿态估计（HeadPoseAnalyzer）

**文件**：`detectors/head_pose_analyzer.py`

**算法**：PnP（Perspective-n-Point）求解 + 欧拉角分解

这是三个检测中最复杂的一个。

### PnP 算法原理

**核心思想**：已知一个标准 3D 人脸模型长什么样，也知道当前 2D 图像上对应的关键点在哪，反推头部在 3D 空间中怎么旋转的。

**标准 3D 人脸模型**（硬编码，单位 mm）：

```python
_MODEL_POINTS = np.array([
    (0.0, 0.0, 0.0),          # 鼻尖      → 对应关键点 #1
    (0.0, -330.0, -65.0),     # 下巴      → 对应关键点 #152
    (-225.0, 170.0, -135.0),  # 左眼角    → 对应关键点 #33
    (225.0, 170.0, -135.0),   # 右眼角    → 对应关键点 #263
    (-150.0, -150.0, -125.0), # 左嘴角    → 对应关键点 #61
    (150.0, -150.0, -125.0),  # 右嘴角    → 对应关键点 #291
])
```

**相机内参矩阵**（假设针孔相机模型）：

```python
focal_length = max(height, width)   # 焦距近似为图像较大边
center = (w/2, h/2)                 # 主点为图像中心

camera_matrix = [
    [focal_length, 0,       center_x],
    [0,            f,       center_y],
    [0,            0,       1       ],
]
dist_coeffs = [0, 0, 0, 0]  # 无畸变
```

**求解**：

```python
success, rotation_vector, translation_vector = cv2.solvePnP(
    _MODEL_POINTS,           # 3D 标准点
    image_points,            # 2D 检测点
    camera_matrix,           # 相机内参
    dist_coeffs,             # 畸变系数
    flags=cv2.SOLVEPNP_ITERATIVE  # 迭代求解
)
```

### 旋转向量 → 欧拉角

`solvePnP` 返回的是旋转向量 (3×1)，需要转成人类可理解的欧拉角：

```python
# 旋转向量 → 3×3 旋转矩阵
rotation_matrix = cv2.Rodrigues(rotation_vector)

# 旋转矩阵 → 欧拉角 (ZYX 分解)
pitch = atan2(-R[2,0], sqrt(R[0,0]² + R[1,0]²))   # 俯仰（点头/低头）
yaw   = atan2(R[1,0], R[0,0])                       # 偏航（左右转头）
roll  = atan2(R[2,1], R[2,2])                       # 翻滚（歪头）

# 弧度 → 角度
pitch, yaw, roll = degrees(pitch), degrees(yaw), degrees(roll)
```

### 疲劳判断逻辑

```python
is_head_down = abs(pitch) > 25.0    # 俯仰角绝对值超过 25°

if is_head_down:
    _frame_counter += 1
else:
    _frame_counter = 0

is_fatigued = _frame_counter >= 50   # 连续 50 帧（~1.67秒 @30fps）→ 低头疲劳
```

**输入**：6 个 2D 关键点 + 图像尺寸
**输出**：`PoseResult(pitch=28.5, yaw=2.1, roll=1.3, is_head_down=True, is_fatigued=True, frame_count=55)`

---

## 六、第⑤步：综合疲劳评估（FatigueEvaluator）

**文件**：`evaluators/fatigue_evaluator.py`

规则模式下逻辑非常简单 — **任一信号触发即为疲劳**（OR 逻辑）：

```python
def _evaluate_rule(eye_result, mouth_result, pose_result):
    reasons = []
    if eye_result.is_fatigued:
        reasons.append("闭眼")
    if mouth_result.is_fatigued:
        reasons.append("哈欠")
    if pose_result.is_fatigued:
        reasons.append("低头")
    return len(reasons) > 0, reasons
```

**输出**：`FatigueStatus(is_fatigued=True, reasons=["闭眼", "哈欠"], mode="rule")`

---

## 七、关于数据集

**规则模式完全不需要数据集。** 所有阈值都是手动设定的经验值：

| 参数 | 默认值 | 来源 |
|------|--------|------|
| EAR 阈值 | 0.2 | 论文经验值 |
| MAR 阈值 | 0.75 | 论文经验值 |
| Pitch 阈值 | 25° | 经验值 |
| 眼部连续帧数 | 48（1.6s @30fps） | 避免正常眨眼误报 |
| 嘴部连续帧数 | 25（0.83s @30fps） | 哈欠持续时间较短 |
| 头部连续帧数 | 50（1.67s @30fps） | 低头持续时间 |

但如果你觉得这些阈值不准，项目提供了 `calibration/threshold_calibrator.py`，可以用带标签的数据集（如 Kaggle DDD、MRL Eye）通过 ROC 曲线自动优化阈值。

---

## 八、完整输入输出总结

```
输入：摄像头一帧图像 (BGR, 640×480)
  │
  ├─ MediaPipe → 468个面部关键点 (像素坐标)
  │
  ├─ EAR计算 → 双眼平均 EAR = 0.15
  │  判断 EAR < 0.2 → 闭眼，连续48帧 → 疲劳 ✓
  │
  ├─ MAR计算 → 嘴巴 MAR = 0.60
  │  判断 MAR < 0.75 → 正常 ✗
  │
  └─ PnP求解 → pitch=28.5°, yaw=2.1°, roll=1.3°
     判断 |pitch| > 25° → 低头，连续50帧 → 疲劳 ✓
  │
  ▼
综合：is_fatigued=True, reasons=["闭眼", "低头"]
  │
  ▼
输出：画面左上角显示 EAR/MAR 数值，画面中央红色大字 "疲劳驾驶！请休息！"
```

简单来说，规则模式就是 **"几何测量 → 阈值比较 → 帧计数器 → OR 逻辑"** 这条流水线，不涉及任何训练过程。
